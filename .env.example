# Hospitable Widget URLs
# ----------------------
# Per-property widget URLs are configured in app/data/properties.ts
# (each property gets its own unique widget — no env var needed for Phase 1)

# Hospitable Public API — Phase 2 only
# (server-only, never exposed to browser)
HOSPITABLE_API_TOKEN=
